import React from 'react';
// import PropTypes from 'prop-types';

// CreateContactsContainer.propTypes = {
    
// };

function CreateContactsContainer(props) {
    return (
        <div>
            this page create contacts
        </div>
    );
}

export default CreateContactsContainer;